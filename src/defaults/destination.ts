import type { DestinationTypes } from '@/types/types';

export const DESTINATION_DATA: DestinationTypes = {
  city: '',
  country: '',
  slogan: '',
  destination_types: [],
  destination_images: [],
};
